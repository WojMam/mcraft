---
interface Props {
  text: string;
  class?: string;
}

const { text, class: className = '' } = Astro.props;
---

<p
  class={`font-display text-xs font-bold uppercase tracking-[0.3em] text-mc-orange ${className}`}
>
  {text}
</p>
