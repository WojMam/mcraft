---
interface Props {
  text: string;
  class?: string;
}

const { text, class: className = '' } = Astro.props;
---

<p
  class={`type-eyebrow text-xs uppercase tracking-[0.3em] text-mc-orange ${className}`}
>
  {text}
</p>
